# Usable.gd - Interface-like class
extends Node

class_name Usable  # So it's globally accessible

# Define the 'use' function with no implementation (interface-like)
func use(manager: Interactions) -> void:
	#get_parent() as 
	pass
